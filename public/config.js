// Set this to the backend API root if the frontend is deployed separately from the API.
// Example:
// window.API_ROOT = 'https://api.myfleetapp.com/api';

window.API_ROOT = window.API_ROOT || '/api';
